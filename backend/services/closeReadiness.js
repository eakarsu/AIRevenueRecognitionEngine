'use strict';
function evaluateCloseReadiness(input={}){const required=['sourceSyncs','journals','postingOutbox','reconciliations','goldenResults'];for(const field of required)if(!Array.isArray(input[field]))throw new Error(`${field} must be an array`);const blockers=[];const unreconciledSyncs=input.sourceSyncs.filter(x=>x.reconciled!==true);if(unreconciledSyncs.length)blockers.push({code:'SOURCE_SYNC_UNRECONCILED',count:unreconciledSyncs.length});const pendingJournals=input.journals.filter(x=>!['reconciled','locked','reversed','restated'].includes(x.state));if(pendingJournals.length)blockers.push({code:'JOURNALS_PENDING',count:pendingJournals.length});const postingFailures=input.postingOutbox.filter(x=>!['delivered','cancelled'].includes(x.status));if(postingFailures.length)blockers.push({code:'POSTING_OUTBOX_PENDING',count:postingFailures.length});const unmatched=input.reconciliations.filter(x=>x.matched!==true);if(unmatched.length)blockers.push({code:'LEDGER_MISMATCH',count:unmatched.length});const goldenPassed=input.goldenResults.some(x=>x.passed===true);if(!goldenPassed)blockers.push({code:'GOLDEN_REPLAY_REQUIRED',count:1});return{schemaVersion:1,periodRef:String(input.periodRef||''),checks:{sourceSyncs:input.sourceSyncs.length,journals:input.journals.length,postingOutbox:input.postingOutbox.length,reconciliations:input.reconciliations.length,goldenReplayPassed:goldenPassed},blockers,readyForControllerReview:blockers.length===0,automaticPeriodLock:false};}
module.exports={evaluateCloseReadiness};
