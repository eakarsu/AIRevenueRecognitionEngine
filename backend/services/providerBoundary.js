'use strict';const NAMES=['CRM','CONTRACTS','BILLING','ERP','USAGE','GENERAL_LEDGER','FX'];function readiness(env=process.env){const providers=NAMES.map(n=>{const enabled=env[`${n}_PROVIDER_ENABLED`]==='true';return{name:n.toLowerCase(),enabled,ready:enabled&&/^https:\/\//.test(String(env[`${n}_PROVIDER_URL`]||''))&&Boolean(env[`${n}_PROVIDER_TOKEN`])};});return{ready:providers.every(x=>x.ready),providers};}function requireProviders(names,env=process.env){const p=readiness(env).providers,wanted=names.map(x=>String(x).toLowerCase()),unavailable=wanted.filter(n=>!p.find(x=>x.name===n&&x.ready));if(unavailable.length)throw Object.assign(new Error(`providers not ready: ${unavailable.join(', ')}`),{code:'PROVIDER_NOT_READY'});return p.filter(x=>wanted.includes(x.name));}module.exports={NAMES,readiness,requireProviders};
